import { ColoresfondoDirective } from './coloresfondo.directive';

describe('ColoresfondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoresfondoDirective();
    expect(directive).toBeTruthy();
  });
});
